name: Update Releases JSON

on:
  schedule:
    - cron: '0 0 * * *'  
  workflow_dispatch:        
  release:
    types: [published]      

permissions:
  contents: write           

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true  

jobs:
  update:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm install

      - name: Generate releases.json
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          node generate-release-json.js
          cp public/releases.json /tmp/releases.json

      - name: Commit and push if changed
        run: |
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"

          git reset --hard HEAD
          git clean -fd

          git pull origin ${{ github.ref_name }} --rebase || true

          cp /tmp/releases.json public/releases.json

          git add public/releases.json

          if git diff --staged --quiet; then
            echo "No changes to commit"
            exit 0
          fi

          git commit -m "Auto update releases.json $(date +'%Y-%m-%d %H:%M:%S')"
          git push --force origin HEAD:${{ github.ref_name }}
